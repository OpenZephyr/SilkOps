# Codex CLI

Discovery: personal skills under `~/.agents/skills`, repository skills under `.agents/skills`, plus OpenAI's own under `~/.codex/skills`. `silkops install-agent codex` links every skill into `~/.agents/skills` (`--scope repo` → `.agents/skills`). Conventions: `AGENTS.md` at the repo root is native (a global `~/.codex/AGENTS.md` applies everywhere). Checked 2026-09-23 against https://github.com/openai/codex/blob/main/docs/skills.md.
