# Cursor

Discovery: `.cursor/skills/` in the repo or `~/.cursor/skills/` for the user; `.agents/skills/` is honoured too (Cursor 2.4+). `silkops install-agent cursor` links every skill into `~/.cursor/skills` (`--scope repo` → `.cursor/skills`); skills also appear in the slash menu as `/<name>`. Conventions: `AGENTS.md` is read. Checked 2026-09-23 against https://cursor.com/docs/skills.
