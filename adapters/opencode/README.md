# OpenCode

Discovery: `.opencode/skills/*/SKILL.md` and `.claude/skills/*/SKILL.md` up the directory path; globally `~/.config/opencode/skills` and `~/.claude/skills`. `silkops install-agent opencode` links every skill into `~/.config/opencode/skills` (`--scope repo` → `.opencode/skills`). Conventions: `AGENTS.md` is native. Checked 2026-09-23 against https://opencode.ai/docs/skills/.
