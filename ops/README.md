# ops

Operations scripts: bash for GitLab operations, Python (3.9 stdlib) for parsers. Every script sources `lib/prelude.sh`, emits one JSON result on stdout and human text on stderr, and uses the fixed exit codes documented in CLAUDE.md.
